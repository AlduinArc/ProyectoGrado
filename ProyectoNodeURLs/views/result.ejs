<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Acortada</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      max-width: 500px;
      margin: 0 auto;
      border-radius: 15px;
    }
    .result-link {
      word-break: break-all;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="card shadow-sm p-4">
      <div class="card-body text-center">
        <h1 class="card-title mb-4">¡URL Acortada!</h1>
        <div class="alert alert-success">
          <p class="mb-2">Copia tu enlace:</p>
          <a 
            href="<%= shortUrl %>" 
            class="result-link d-block mb-3" 
            target="_blank"
          >
            http://localhost:3000/<%= shortUrl %>
          </a>
          <button 
            onclick="navigator.clipboard.writeText('http://212.56.46.43:3000/<%= shortUrl %>')" 
            class="btn btn-sm btn-outline-secondary"
          >
            Copiar
          </button>
        </div>
        <a href="/" class="btn btn-primary mt-3">
          Acortar otra URL
        </a>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (para funcionalidad del botón "Copiar") -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>